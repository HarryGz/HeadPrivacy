# HeadPrivacy 手动验收清单

本清单用于真实 AirPods、Mac 和显示器上的最终验收。自动化证据与尚未执行的硬件/UI 检查严格分开记录；任何空白项或 `NOT RUN` 都不能视为通过。

## 本次运行信息

| 项目 | 记录 |
|---|---|
| 日期 | 2026-09-18 |
| Mac 型号 | NOT RECORDED |
| macOS | 26.6.2 |
| CPU 架构 | arm64 |
| AirPods 型号 / 固件 | NOT RECORDED |
| 显示器型号 / 左右排列 | NOT RECORDED |
| App commit / 版本 | `2dc4989` / `0.1.0` |
| 执行人 | NOT RECORDED |

状态只能填写：

- `PASS`：实际结果满足预期，并记录了可核查证据。
- `FAIL`：实际结果不满足预期；必须附现象、复现步骤和证据。
- `BLOCKED`：因明确的环境、权限或硬件问题无法执行；必须写明阻碍。
- `NOT RUN`：尚未执行。

规则：空白或 `NOT RUN` 永远不能视为 `PASS`。延迟目标必须使用可复核的录屏逐帧、时间戳日志或高速摄像测量，不凭主观感觉判断。

## 自动化证据（不替代真实硬件/UI 验收）

以下证据只覆盖构建、单元/集成测试和静态 bundle 合同，不证明真实 AirPods、窗口合成、Spaces、系统权限界面或实际延迟已经通过。

| 检查 | 证据 | 状态 |
|---|---|---|
| Swift 测试套件 | `swift test --disable-sandbox`：153 tests，0 failures；2026-09-18 | PASS |
| arm64 Release app bundle | `./Scripts/build-app.sh`：exit 0，产物为 `build/HeadPrivacy.app`；2026-09-18 | PASS |
| Info.plist 语法 | `plutil -lint build/HeadPrivacy.app/Contents/Info.plist`：`OK` | PASS |
| ad-hoc 签名完整性 | `codesign --verify --deep --strict --verbose=2 build/HeadPrivacy.app`：`valid on disk`、`satisfies its Designated Requirement` | PASS |
| 可执行文件架构 | `file`：`Mach-O 64-bit executable arm64`；`lipo -archs`：`arm64` | PASS |
| 权限键静态检查 | 对 `Config/Info.plist` 与 bundle plist 执行 `plutil -p` 和敏感权限键检索：两者一致且只有非空 `NSMotionUsageDescription`，未发现 Screen Recording、Accessibility、相机、麦克风、定位或网络权限说明键 | PASS |

## 硬件与 UI 验收

本记录创建时没有可由自动化代理实际佩戴并转动的 AirPods，也没有可人工操作的多显示器桌面会话。因此下列各项均保持 `NOT RUN`；观察结果不得用单元测试推断填充。

| ID | 精确设置 | 操作 | 预期结果 | 实际结果 / 证据 | 延迟 | 状态 |
|---|---|---|---|---|---|---|
| H01 | Apple-silicon MacBook；内置显示器启用且作为唯一活动显示器；连接普通 AirPods 第 3 代或更新型号；Motion 已允许；完成单屏校准 | 佩戴耳机，缓慢左右转头，再做一次大幅转头 | App 获得连续 yaw；菜单状态随水平朝向变化；不使用 pitch/roll 选择屏幕 | 未执行；MacBook/AirPods 身份未记录 | 记录首次样本及状态变化时间 | NOT RUN |
| H02 | Apple-silicon MacBook；内置显示器启用且作为唯一活动显示器；连接任一代 AirPods Pro；Motion 已允许；完成单屏校准 | 重复 H01 的水平转头 | Core Motion 运行时能力可用，状态随 yaw 更新 | 未执行；MacBook/AirPods 身份未记录 | 记录首次样本及状态变化时间 | NOT RUN |
| H03 | 全新权限状态；已构建并启动本地 app | 在系统弹窗允许 Motion，然后佩戴支持的 AirPods 并校准 | 仅出现 Motion 请求；允许后可进入校准并获得样本 | 未执行 | 不适用 | NOT RUN |
| H04 | Motion 权限已拒绝 | 启动 app；查看指引；到系统设置重新允许；返回并重新校准 | 拒绝时不启动保护且明确提示；重新允许后可恢复，不复用不安全校准 | 未执行 | 记录恢复到可校准的时间 | NOT RUN |
| H05 | 单显示器；已校准；Side + Translucent；录屏或高速摄像 | 从屏幕中心大幅转向所有校准区之外 | 该显示器进入保护；从越过阈值到遮罩可见小于 250 ms | 未执行 | 目标 `<250 ms`；实测：— | NOT RUN |
| H06 | 延续 H05，显示器当前已保护 | 转回已校准中心区域 | 当前显示器恢复清晰；从进入区域到清晰小于 200 ms | 未执行 | 目标 `<200 ms`；实测：— | NOT RUN |
| H07 | Apple-silicon MacBook 内置显示器保持启用；另接两台外接显示器；三台显示器水平排列，系统排列与物理左右顺序一致 | 按引导从左到右校准；第一屏捕获后继续看第一屏至少一秒，确认第二屏未采样；转向第二屏并按“I'm Looking Here — Start Sampling”；第三屏重复 | 引导顺序为左→中→右；新屏必须显式确认后才开始新的稳定窗口；三个中心均成功保存 | 未执行；MacBook/显示器未记录 | 不适用 | NOT RUN |
| H08 | 延续 H07；自动保护开启 | 依次稳定看左、中、右显示器 | 每次仅正在看的显示器清晰，其他两台被保护 | 未执行 | 每次记录状态切换延迟 | NOT RUN |
| H09 | 延续 H07；自动保护开启 | 看向所有已校准显示区域之外并保持 | 所有显示器均被保护 | 未执行 | 记录 away 触发延迟 | NOT RUN |
| H10 | 清除偏好后首次启动 | 打开“保护”设置页 | 默认模式显示为 Side；侧边区域遮挡、屏幕中部保留 | 未执行 | 不适用 | NOT RUN |
| H11 | 已完成校准；保护模式切为 Full-screen | 触发另一屏或 away 保护 | 被保护显示器整屏遮挡，当前查看屏保持清晰 | 未执行 | 记录更新延迟 | NOT RUN |
| H12 | Side 模式 | 依次选择 Soft、Translucent、Privacy 并触发保护 | 三种外观均可辨识且符合强度递增预期；交互不被遮罩窗口抢占 | 未执行 | 不适用 | NOT RUN |
| H13 | 清除偏好后首次启动 | 打开“保护”设置页 | 默认预设为 Translucent | 未执行 | 不适用 | NOT RUN |
| H14 | 已校准并稳定看当前屏 | 做小幅左右摆头但不越过区域边界 | 不切换屏幕、不闪烁保护状态 | 未执行 | 观察至少 30 秒 | NOT RUN |
| H15 | 已校准并稳定看当前屏 | 只点头、尽量保持水平朝向 | 不因 pitch 变化切换屏幕或触发 away | 未执行 | 观察至少 10 次点头 | NOT RUN |
| H16 | 使用可跨越 ±180° 相对 yaw 的布置/动作；录制状态 | 缓慢越过 wraparound 边界 | 角度连续，不绕经 0° 误判到远端显示器 | 未执行 | 记录状态轨迹 | NOT RUN |
| H17 | 头部位于两个区域边界附近 | 在边界两侧做小幅抖动，再稳定越界 | 3° 滞回抑制抖动；只有稳定超过 dwell 后切换 | 未执行 | 记录 dwell 与切换时间 | NOT RUN |
| H18 | 至少两台已校准显示器；自动保护开启 | 热插拔一台显示器，随后再快速调整一次排列 | 拓扑失效，自动分类暂停并只呈现一个完整重校准面板；不静默复用映射 | 未执行 | 记录检测时间 | NOT RUN |
| H19 | 至少两台已校准显示器 | 在系统设置中交换显示器左右排列 | 旧校准失效并启动验证/校准流程 | 未执行 | 记录检测时间 | NOT RUN |
| H20 | 已校准、自动保护开启 | 让 Mac 睡眠后唤醒 | 睡眠前停止 motion；唤醒后重新获取，并只呈现一个完整重校准面板；新样本前遵循当前失败策略 | 未执行 | 记录恢复时间 | NOT RUN |
| H21 | 已校准、自动保护开启；Usability-first | 取下 AirPods 或断开连接，再重新佩戴/连接 | 中断时清除遮罩并提示；恢复后要求安全恢复流程，不复用无效参考 | 未执行 | 记录中断与恢复时间 | NOT RUN |
| H22 | Usability-first；已进入可保护状态 | 制造耳机断开、Motion 不可用或样本超时 | 默认行为为显示器清晰并提示中断 | 未执行 | 记录 500 ms stale 后行为 | NOT RUN |
| H23 | Protection-first；已进入可保护状态 | 制造耳机断开、Motion 不可用或样本超时；分别使用菜单“Pause Protection”“Temporarily Reveal All”和全局快捷键 | 所有显示器先被保护并显示 app 自有状态说明；说明不泄露屏幕内容且不抢焦点/鼠标；三种逃生操作均立即显示全部；校准恢复前 Resume 保持禁用 | 未执行 | 记录 500 ms stale 及逃生响应时间 | NOT RUN |
| H24 | 清除偏好后首次启动 | 打开“失败行为”设置 | 默认选择 Usability-first；可切换 Protection-first | 未执行 | 不适用 | NOT RUN |
| H25 | 打开原生全屏应用；已完成校准 | 转向另一显示器或 away | 遮罩覆盖全屏应用，当前查看屏仍清晰 | 未执行 | 记录切换延迟 | NOT RUN |
| H26 | 创建至少两个 macOS Spaces；已完成校准 | 在各 Space 切换并分别触发保护 | 遮罩在所有 Spaces 正确覆盖且不泄漏旧状态 | 未执行 | 不适用 | NOT RUN |
| H27 | 在清晰显示器的文本框中输入；显示另一屏遮罩 | 连续键入并点击/拖动；显示菜单栏菜单 | 键盘焦点与鼠标操作不被 overlay 抢占；菜单可操作 | 未执行 | 不适用 | NOT RUN |
| H28 | 已校准并运行；默认快捷键未冲突 | 使用菜单暂停/恢复、临时显示全部、重新校准；按 `⌃⌥⌘P` | 命令均生效；全局快捷键可在其他 app 前台时暂停/恢复 | 未执行 | 记录快捷键响应时间 | NOT RUN |
| H29 | 系统已存在占用 `⌃⌥⌘P` 的快捷键或构造冲突 | 启动 app；查看错误；更换快捷键或点击重试 | 冲突清晰呈现；无关设置变化不重复注册；解除冲突后重试成功 | 未执行 | 不适用 | NOT RUN |
| H30 | App 在后台；Settings 窗口已关闭或被遮挡 | 从菜单选择 Settings | App 激活，Settings 窗口进入前台 | 未执行 | 不适用 | NOT RUN |
| H31 | “登录时启动”初始关闭 | 快速切换 开→关→开，等待系统调用结束；重启会话验证 | 最终系统登录项与最后一次“开”一致，无陈旧错误 | 未执行 | 记录最终收敛时间 | NOT RUN |
| H32 | 系统设置“隐私与安全性”可见；app 已运行并触发 Motion 请求 | 检查 Motion & Fitness、Screen Recording 及其他隐私列表 | HeadPrivacy 只出现在 Motion；不请求/不出现在 Screen Recording、Accessibility、相机、麦克风、定位 | 未执行 | 不适用 | NOT RUN |
| H33 | 通知尚未授权；设置中通知开关可见 | 仅启动 app，不按授权按钮；随后按“请求通知权限” | 启动本身不弹通知授权；仅按按钮时显式请求 | 未执行 | 不适用 | NOT RUN |
| H34 | 已完成多屏校准 | 退出并重启 app，进入每次启动必需的引导验证 | 启动后不会直接自动保护；须重新建立相对头部参考；保存的每屏区域宽度在验证后保留 | 未执行 | 不适用 | NOT RUN |
| H35 | 自动保护运行中 | 正常退出 app；另一次运行用可控方式终止进程模拟崩溃 | 两种情况下 overlay 随进程消失；文档明确这不是安全锁屏 | 未执行 | 记录窗口消失行为 | NOT RUN |
| H36 | 已完成三屏校准且 tracking 正常；三台显示器区域宽度各不相同 | 在 Displays 设置对中间屏选择“Recalibrate This Display…”；显式确认后采样、验证并保存 | 不重新捕获 motion 参考；仅中间屏中心改变；三台区域宽度和其他两屏中心不变；取消一次后原校准仍可继续保护 | 未执行 | 不适用 | NOT RUN |
| H37 | 已校准并运行；连接至少一台外接显示器 | 关闭 MacBook 内置显示器，进入仅外接布局；观察菜单/设置和校准入口 | 自动分类停止；显示“必须恰好一个 MacBook 内置显示器”的可操作错误；要求完整重校准且不允许单屏重校准 | 未执行 | 记录失效检测时间 | NOT RUN |

## 结果处理

- 任一 `FAIL`：保留录屏、日志、系统信息与复现步骤；若可在无硬件条件下重现，先加入失败的自动化测试，再做最小修复，然后重跑受影响硬件项。
- 任一 `BLOCKED`：记录缺失的设备、权限或环境，以及解除阻碍所需动作。
- 全部硬件/UI 行均为 `PASS` 前，不得宣称真实硬件验收完成。
